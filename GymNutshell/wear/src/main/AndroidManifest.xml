<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android">

    <!-- Declara que este APK só roda em relógios Wear OS. -->
    <uses-feature android:name="android.hardware.type.watch" />

    <!-- Ícone circular azul (mesma arte do watchOS, que a Apple aprovou após rejeitar
         a versão preta por se misturar com o fundo do relógio). Raster em todas as
         densidades — o Wear OS recorta em círculo e renderiza de forma previsível. -->
    <application
        android:icon="@mipmap/ic_launcher"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:label="@string/app_name"
        android:theme="@android:style/Theme.DeviceDefault">

        <!-- Biblioteca do Wear (opcional pra apps standalone, mas recomendada). -->
        <uses-library
            android:name="com.google.android.wearable"
            android:required="false" />

        <!-- standalone=true: o app do relógio funciona sem o celular por perto
             (igual ao Watch app do iOS, que tem estado local próprio). -->
        <meta-data
            android:name="com.google.android.wearable.standalone"
            android:value="true" />

        <activity
            android:name=".MainActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <!-- Recebe os DataItems do celular (snapshot), mesmo com o app fechado. -->
        <service
            android:name=".sync.WearDataListenerService"
            android:exported="true">
            <intent-filter>
                <action android:name="com.google.android.gms.wearable.DATA_CHANGED" />
                <data
                    android:scheme="wear"
                    android:host="*"
                    android:pathPrefix="/gymnutshell" />
            </intent-filter>
        </service>

        <!-- Tile de progresso (porte da complication do iOS). -->
        <service
            android:name=".tile.ProgressTileService"
            android:exported="true"
            android:label="@string/app_name"
            android:permission="com.google.android.wearable.permission.BIND_TILE_PROVIDER">
            <intent-filter>
                <action android:name="androidx.wear.tiles.action.BIND_TILE_PROVIDER" />
            </intent-filter>
        </service>
    </application>
</manifest>
